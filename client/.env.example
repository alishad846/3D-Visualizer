# ScanVista Client — copy to client/.env for local development
# Production (Render): set VITE_API_URL in the static site service env vars

VITE_API_URL=http://localhost:5000/api

# Optional — future AI service (not wired in current client)
# VITE_AI_SERVICE_URL=http://localhost:8000
